# Simple-Metadata-Viewer
A simple Python application with a Tkinter GUI that takes in any file as an input and outputs its metadata and sha256 hash.

![Extracting metadata from Windows 11 ISO file](../assets/Example.png)
<p align="center">Extracting metadata from Windows 11 ISO file</p>